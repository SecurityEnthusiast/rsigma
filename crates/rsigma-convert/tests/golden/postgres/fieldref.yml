title: Fieldref
logsource:
    category: process_creation
    product: windows
detection:
    selection:
        Image|fieldref: ParentImage
        User|fieldref|cased: TargetUser
        CommandLine|fieldref|contains: Token
        Path|fieldref|startswith: Folder
        File|fieldref|endswith|cased: Name
    condition: selection
